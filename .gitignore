# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Windows / OneDrive
Thumbs.db
ehthumbs.db
desktop.ini
$RECYCLE.BIN/

# Cache incremental do TypeScript
*.tsbuildinfo

# Worktrees e área de trabalho dos agentes
.worktrees/
.superpowers/

# Banco de usuários/sessões — contém hash de senha, não é dado de exemplo
server/data/

# Variáveis da stack de produção (docker-compose.yml) — domínio e origens,
# nada secreto aqui hoje, mas .env não é pra ser versionado por convenção
.env
